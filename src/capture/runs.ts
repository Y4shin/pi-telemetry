import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import type { Telemetry } from "../state.ts";

export function registerRunCapture(_pi: ExtensionAPI, _t: Telemetry): void {
  // TODO
}
